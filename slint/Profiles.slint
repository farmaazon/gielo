import { PlayerSkills } from "./GameModel.slint";

export global Profiles := {
    property <[string]> player-skills: ["Custom"];
    property <[string]> rules: ["Default"];
    property <[string]> ice: ["Default"];

    callback load-player-skills(int) -> PlayerSkills;
    callback custom-player-skills-label(PlayerSkills) -> string;
}